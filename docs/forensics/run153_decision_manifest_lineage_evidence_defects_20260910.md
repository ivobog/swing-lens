# Run 153 decision-manifest, lineage, and evidence defect register

Certification date: 2026-09-10. Baseline: `0363a1483b747ee8f551766421e4fc04704d912f` on production schema `0071_transition_preflight_plan`. Production was queried read-only; every mutation and migration exercise used `swinglens_pytest_run153_remediation_clone` or a test database.

## Findings

| ID | Severity | Workstream | Invariant violated | Root cause and reproducer | Affected paths | Remediation and proof | Status |
|---|---|---|---|---|---|---|---|
| TPI-DM-F001 | Critical | Decision manifest | TPI-INV-PROD-EQ | TBLA bars 1786648/1786650 were known before Context 6 but revised afterward. Both old PIT loaders rejected the mutable current rows because `revised_at > cutoff`, instead of projecting revision rows 34268/34269 `previous_values_json`. Preflight retained 2026-08-03; execution fell back to 1786647/1786649 on 2026-07-31. | `price_bar_repository.py`, technical and lifecycle bar loaders | Central as-of projection reconstructs the last version known at cutoff, preserving Decimal values, identity and PIT metadata. Exact production read-only reconstruction and clone replay cover it. | FIXED |
| TPI-DM-F002 | Critical | Decision manifest | TPI-INV-PROD-EQ | Plan 2 stored only partial candidate/evidence fingerprints. Production lifecycle capture could derive another key without an exact semantic comparison. Run 153 created TBLA snapshot 36950 at 2026-07-31 rather than failing. | candidate discovery, preflight plan, lifecycle capture | Added canonical `TransitionDecisionManifest` (Model B). The immutable plan stores it; enqueue revalidates it; capture reconstructs it through production services and raises `DECISION_MANIFEST_MISMATCH` before evaluation-run, snapshot, pointer, ledger, or decision-linked writes. Injected clone mismatch left all three lifecycle table counts unchanged. | FIXED |
| TPI-DM-F003 | High | Decision manifest | Same inputs must use the same decision builder | Preflight called the snapshot builder per ticker without the production history/velocity construction used at capture. | `transition_candidate_service.py`, `snapshot_builder.py` | Both paths now call `build_run_context_snapshots`, including identical history and velocity queries and ordering. | FIXED |
| TPI-DM-F004 | High | Decision manifest | Frozen context must survive every technical execution path | A forced process-pool failure exposed that sequential fallback omitted the required `market_cutoff`, producing a `TypeError` instead of an equivalent result. | `technical_score_service.py` | Fallback now forwards the same frozen context. Broad production-path clone replay exercised the fallback-capable production scorer. | FIXED |
| TPI-CERI-LIN-F001 | Critical | CERI lineage | Pipeline temporal children require explicit context | Run 153 jobs 42973/42975 carried Context 6, cutoff and calendar, but `CeriFeatureRebuildRequest` and the batch context discarded context/calendar before child construction. Rows 8684/8685 therefore stored NULL context. | batched and legacy CERI job handlers, feature rebuild request/context | Pipeline handlers reject incomplete context payloads and pass explicit `PIPELINE` ownership, context, cutoff and calendar through request and batch context. | FIXED |
| TPI-CERI-LIN-F002 | Critical | CERI lineage | Retry/update must preserve lineage | Direct capture supplied context, but `CeriPriceResponseService.persist` looked up the context-independent event key and updated metrics only; an existing NULL-lineage row stayed NULL. | price-response build/persist and capture | Pipeline event identity includes context; all non-administrative fields are copied on retry/update; evidence hashes bind context/cutoff/calendar/ownership. Two-session clone replay preserved IDs and lineage. | FIXED |
| TPI-CERI-LIN-F003 | High | CERI lineage | Every pipeline-derived feature family requires owner identity | Revision features, derived features and build states had no context column or ownership classification, so pipeline and standalone identities could collide. | CERI models, batch preload/upserts, revision hashing | Migration 0072 adds nullable lineage plus `ownership_mode`, FKs, indexes, context-aware unique identities, an ownership enum constraint, and conditional PIPELINE context constraints. Existing rows remain `LEGACY_UNKNOWN`; no lineage was fabricated. | FIXED |
| TPI-CERI-LIN-F004 | High | CERI lineage | Standalone must be explicit, not optional by accident | Optional defaults and a standalone existing-feature lookup could match a pipeline row; an arbitrary ownership string could also bypass a PIPELINE-only DB check. | artifact lineage validator, feature rebuild lookup, migration constraints | Standalone is an explicit enum value, lookups include ownership/context, and DB constraints admit only PIPELINE/STANDALONE/LEGACY_UNKNOWN. | FIXED |
| TPI-HASH-F001 | High | Evidence classification | Operational observation metadata must not fail historical-integrity gates | Bars 2263479/2263504 changed only `last_seen_at` after identical IB re-observation. Old full-row checks reported historical mutation even though OHLCV, source identity and PIT provenance were byte-equivalent. | price-bar evidence/certification checks | Added the v1 immutable-evidence field contract and kept a separate full-row diagnostic. Unit and production/backup comparison prove immutable hashes remain equal while diagnostics change. | FIXED |
| TPI-HASH-F002 | High | Adjacent hash audit | All PASS/FAIL price controls must use the certified contract | Winner maturation and candidate-estimate scripts hashed complete `price_bars` rows and would repeat the Run 153 false positive. | `winner_maturation_canary.py`, `winner_candidate_estimates.py` | PASS/FAIL now compares immutable evidence; full-row hashes remain reported under explicit diagnostic keys and do not block on `last_seen_at` alone. | FIXED |
| TPI-LIN-F001 | High | Adjacent lineage | Shared PIT consumers must reconstruct known historical versions | Setup lifecycle and CERI price loaders repeated the post-cutoff-revision rejection pattern. | lifecycle source loader, CERI price-response loader, batched feature loader | All use the shared as-of revision projection. Material revisions remain detectable and post-cutoff values cannot leak into frozen computation. | FIXED |

## Blocking status

Eleven blocking/high findings were found and fixed. No blocking defect remains in the audited decision-manifest, CERI temporal-artifact, price-evidence, adjacent temporal-lineage, or adjacent price-hash paths. Legacy CERI rows are deliberately labelled `LEGACY_UNKNOWN`; migration 0072 does not claim or fabricate their historical owner.

# Legacy immutability

Legacy snapshot 1948 matches all `26` fields in the frozen forensic export; mismatch count is `0`. New additive columns remain null and the original hash is unchanged. See [legacy_immutability.json](data/legacy_immutability.json).

# Estimate normalization

Missing currency is no longer inferred from a `.US` ticker. Currency provenance and verification are persisted; incomparable records remain unavailable. NWE has 0 verified comparable legacy estimates in the frozen evidence set, so no revision evidence was fabricated. See [nwe_estimate_normalization.json](data/nwe_estimate_normalization.json).

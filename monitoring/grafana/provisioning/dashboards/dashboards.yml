apiVersion: 1
providers:
  - name: SwingLens
    orgId: 1
    folder: SwingLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
